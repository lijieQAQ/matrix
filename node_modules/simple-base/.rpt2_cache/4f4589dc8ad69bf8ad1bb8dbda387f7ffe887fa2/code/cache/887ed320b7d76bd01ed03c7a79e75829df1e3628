{"code":"import encode from './encode';\r\nimport decode from './decode';\r\nimport alphabet from './alphabet/constants';\r\nfunction curry(fn, base) {\r\n    return function (param) { return fn(param, base); };\r\n}\r\nvar lib = {\r\n    encode: encode,\r\n    decode: decode\r\n};\r\nObject.keys(alphabet).forEach(function (base) {\r\n    lib[\"base\" + base] = {\r\n        encode: curry(encode, parseInt(base, 10)),\r\n        decode: curry(decode, parseInt(base, 10))\r\n    };\r\n});\r\nexport default lib;\r\n","map":{"mappings":""}}
